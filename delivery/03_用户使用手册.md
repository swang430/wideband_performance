# 终端宽带标准信道验证系统 - 用户使用手册

**项目版本**: v1.0  
**手册版本**: 1.0  
**最后更新**: 2026-01-17  
**适用人群**: 测试工程师、研发人员

---

## 目录

1. [快速开始](#1-快速开始)
2. [系统安装](#2-系统安装)
3. [前端界面操作](#3-前端界面操作)
4. [场景编写指南](#4-场景编写指南)
5. [进阶使用](#5-进阶使用)
6. [常见问题 FAQ](#6-常见问题-faq)
7. [故障排除](#7-故障排除)

---

## 1. 快速开始

### 1.1 5分钟快速上手

#### 步骤 1: 启动系统

```bash
cd /Users/Simon/Tools/WideBand_Performance
./start_system.sh
```

> 🎉 **成功提示**: 看到以下输出表示启动成功：
> ```
> ✅ 后端服务正在运行 (PID: 12345)
> ✅ 前端服务正在运行 (PID: 12346)
> ```

#### 步骤 2: 打开浏览器

访问: http://localhost:5173

#### 步骤 3: 运行第一个测试

1. 选择**场景**: 下拉菜单 → `static_awgn.yaml`
2. 点击 **▶️ 启动测试**
3. 观察 **实时日志** 和 **吞吐量曲线**
4. 等待测试完成（约 30 秒）

**恭喜！** 🎉 您已成功运行第一个场景！

---

## 2. 系统安装

### 2.1 系统要求

| 组件 | 最低要求 | 推荐配置 |
|------|---------|---------|
| **操作系统** | macOS 12+ / Ubuntu 20.04+ | macOS 14+ |
| **Python** | 3.10+ | 3.13 |
| **Node.js** | 18.x | 20.x |
| **内存** | 4 GB | 8 GB |
| **磁盘** | 2 GB | 5 GB |

### 2.2 完整安装步骤

#### 2.2.1 克隆项目

```bash
git clone https://github.com/swang430/wideband_performance.git
cd wideband_performance
```

#### 2.2.2 安装后端依赖

```bash
# 创建虚拟环境
python3 -m venv venv

# 激活虚拟环境
source venv/bin/activate  # macOS/Linux
# 或
venv\Scripts\activate     # Windows

# 安装依赖
cd backend
pip install -r requirements.txt
```

#### 2.2.3 安装前端依赖

```bash
cd ../frontend
npm install
```

#### 2.2.4 初始化数据库

```bash
cd ../backend
python -c "from app.database import init_database; init_database()"
```

### 2.3 启动服务

#### 方式 1: 使用脚本（推荐）

```bash
./start_system.sh
```

#### 方式 2: 手动启动

**后端**:
```bash
source venv/bin/activate
cd backend
uvicorn app.main:app --host 127.0.0.1 --port 8000
```

**前端**:
```bash
cd frontend
npm run dev
```

### 2.4 验证安装

访问 http://localhost:5173，看到主界面即安装成功。

---

## 3. 前端界面操作

### 3.1 主控制面板 (Dashboard)

![Dashboard 布局](dashboard-layout.png)

#### 界面分区

```
┌─────────────────────────────────────────────────┐
│  导航栏: Home | Config | History | Models       │
├───────────────┬─────────────────────────────────┤
│ 仪表状态面板   │  DUT 状态面板                    │
│ (左上)        │  (右上)                         │
├───────────────┴─────────────────────────────────┤
│           测试进度条 + 时间轴事件                │
├─────────────────────────────────────────────────┤
│           实时数据曲线 (吞吐量/BLER)             │
├─────────────────────────────────────────────────┤
│           实时日志窗口                           │
└─────────────────────────────────────────────────┘
```

#### 仪表状态面板

显示 5 个仪表的连接状态：

| 指示灯 | 含义 |
|-------|------|
| 🟢 绿色 | 已连接 |
| 🔴 红色 | 未连接 |
| 🟡 黄色 | 连接中 |

**点击卡片** 可查看仪表详细信息（厂商、型号、序列号）。

#### DUT 状态面板

显示 Android 终端信息：
- **连接状态**: 已连接 / 未连接
- **信号质量**: RSRP / SINR / CQI
- **飞行模式开关**: 点击切换

#### 测试控制区

1. **场景选择**: 下拉菜单选择测试场景
2. **启动测试**: ▶️ 按钮
3. **停止测试**: ⏸️ 按钮
4. **进度指示**: 显示当前时间 / 总时长

#### 实时数据曲线

- **蓝色线**: 吞吐量 (Mbps)
- **红色线**: BLER (误块率)
- **自动刷新**: 每秒更新

#### 日志窗口

- **自动滚动**: 最新日志在底部
- **日志级别**:
  - `>>>` 场景事件
  - `[仪表名]` 设备操作
  - `❌` 错误信息

---

### 3.2 配置编辑器 (ConfigEditor)

![ConfigEditor 界面](configeditor.png)

#### 功能说明

**左侧**: YAML 编辑器
- 语法高亮
- 代码折叠
- 查找替换 (Ctrl/Cmd + F)

**右侧**: 参数帮助面板
- 场景类型说明
- 参数示例
- 常用模板

#### 操作步骤

1. **选择场景**: 左上角下拉菜单
2. **编辑内容**: 在编辑器中修改
3. **保存**: 点击 💾 保存按钮
4. **新建**: 点击 ➕ 新建场景

> **提示**: 编辑器自动保存到 `backend/scenarios/` 目录

---

### 3.3 测试历史 (TestHistory)

![TestHistory 表格](testhistory.png)

#### 表格列说明

| 列名 | 说明 |
|------|------|
| **ID** | 测试运行 ID |
| **场景名称** | 执行的场景 |
| **开始时间** | 测试开始时间 |
| **结束时间** | 测试结束时间 |
| **状态** | completed / failed / running |
| **操作** | 查看详情 / 生成报告 |

#### 操作指南

**查看详情**:
- 点击 👁️ 图标
- 查看完整测试数据和指标曲线

**生成报告**:
- 点击 📄 图标
- 自动生成 HTML 报告
- 下载保存

---

### 3.4 信道模型 (ChannelModels)

![ChannelModels 页面](channelmodels.png)

#### 功能说明

**统计卡片**:
- 场景总数
- 模型总数
- 模型使用次数

**柱状图**: 模型使用频率排名
**饼图**: 模型分布占比
**详情表**: 场景与模型对应关系

> **用途**: 快速了解哪些信道模型被频繁使用，便于测试规划

---

## 4. 场景编写指南

### 4.1 场景文件结构

所有场景文件位于: `backend/scenarios/*.yaml`

#### 基本结构

```yaml
metadata:
  id: "UNIQUE_ID"           # 唯一标识
  name: "场景名称"           # 显示名称
  version: "1.0"            # 版本号
  author: "作者"            # 作者

config:
  type: "场景类型"          # 必填
  # ... 其他配置
```

### 4.2 场景类型

#### 4.2.1 静态场景 (static)

**用途**: 固定参数的基准测试

**示例**:
```yaml
config:
  type: "static"
  duration: 30                    # 测试时长(秒)
  carrier_freq_hz: 3500e6         # 载波频率
  bandwidth_mhz: 100              # 带宽
  cell_power_dbm: -85.0           # 小区功率
  awgn_snr_db: 15.0               # 信噪比
  
  metrics:
    interval: 1.0                 # 采样间隔
    targets: ["integrated_tester"] # 采样目标
```

#### 4.2.2 动态场景 (dynamic_scenario)

**用途**: 时间轴事件驱动的动态测试

**示例**:
```yaml
config:
  type: "dynamic_scenario"
  total_duration: 60
  
  timeline:
    - time: 0                     # 时间偏移(秒)
      target: "channel_emulator"  # 目标仪表
      action: "load_channel_model" # 动作方法
      params: { model: "3GPP_UMA_SCENARIO.scn" }
      comment: "加载信道模型"      # 可选注释
      
    - time: 5
      target: "channel_emulator"
      action: "set_path_loss"
      params: { db: 85 }
      comment: "设置路损"
  
  metrics:
    interval: 0.2                 # 高频采样
```

#### 4.2.3 灵敏度测试 (sensitivity)

**用途**: 自动搜索灵敏度阈值

**示例**:
```yaml
config:
  type: "sensitivity"
  strategy: "algorithm"
  
  carrier_freq_hz: 3500e6
  bandwidth_mhz: 100
  
  search:
    power_min: -110
    power_max: -60
    power_step: 1
    bler_threshold: 0.1           # BLER 门限
```

### 4.3 参数命名规范

> ⚠️ **重要**: 参数名必须与驱动方法签名匹配！

#### 常用参数对照表

| 驱动方法 | 参数名 | 类型 | 示例 |
|---------|-------|------|------|
| `load_channel_model()` | `model` | str | `"3GPP_UMA.scn"` |
| `set_path_loss()` | `db` | float | `85.0` |
| `set_velocity()` | `kmh` | float | `120.0` |
| `set_frequency()` | `hz` | float | `3.5e9` |
| `set_power()` | `dbm` | float | `-50.0` |

> **查看驱动方法签名**: 
> ```bash
> grep "def " backend/drivers/channel_emulator.py
> ```

### 4.4 场景编写最佳实践

#### ✅ 推荐做法

1. **清晰的注释**: 每个事件添加 `comment` 字段
2. **合理的时间间隔**: 避免事件过于密集
3. **适当的采样频率**: 
   - 静态场景: 1s
   - 动态场景: 0.2-0.5s
4. **使用模板**: 复制现有场景修改

#### ❌ 常见错误

1. **参数名拼写错误**:
   ```yaml
   # ❌ 错误
   params: { velocity_kmh: 350 }
   
   # ✅ 正确
   params: { kmh: 350 }
   ```

2. **时间顺序混乱**:
   ```yaml
   # ❌ 错误 (time 不递增)
   - time: 10
   - time: 5  # 应该在前面
   
   # ✅ 正确
   - time: 5
   - time: 10
   ```

3. **单位错误**:
   ```yaml
   # ❌ 错误 (频率单位应为 Hz)
   carrier_freq_hz: 3500  # 这是 3.5 kHz！
   
   # ✅ 正确
   carrier_freq_hz: 3500e6  # 3500 MHz = 3.5 GHz
   ```

---

## 5. 进阶使用

### 5.1 模拟模式 vs 真实模式

#### 模拟模式 (simulation=True)

**优点**:
- ✅ 无需硬件，快速开发
- ✅ 日志清晰，便于调试
- ✅ 可重复性好

**局限**:
- ❌ 无法验证真实 SCPI 命令
- ❌ 无法采集真实数据

**使用场景**: 开发、调试、演示

#### 真实模式 (simulation=False)

**前提条件**:
1. 所有仪表正确连接
2. 网络配置正确
3. VISA 驱动已安装

**配置方法**:
```python
# 方式 1: 环境变量
export SIMULATION_MODE=0

# 方式 2: 代码
sequencer = TestSequencer(config, simulation_mode=False)
```

### 5.2 自定义驱动

如果需要支持新的仪表厂商：

#### 步骤 1: 创建驱动类

```python
# backend/drivers/my_vendor/my_instrument.py
from drivers.base_instrument import BaseInstrument

class MyInstrument(BaseInstrument):
    def __init__(self, resource_id: str, simulation: bool = False):
        self.resource_id = resource_id
        self.simulation = simulation
        if not simulation:
            import pyvisa
            rm = pyvisa.ResourceManager()
            self.inst = rm.open_resource(resource_id)
    
    def connect(self) -> None:
        if self.simulation:
            print(f"[模拟] 连接到 {self.resource_id}")
        else:
            # 真实连接逻辑
            self.inst.write("*RST")
    
    def my_custom_method(self, param: float):
        if self.simulation:
            print(f"[模拟] 自定义方法: {param}")
        else:
            self.inst.write(f"CUSTOM:PARAM {param}")
```

#### 步骤 2: 注册驱动

```python
# backend/drivers/factory.py
from drivers.my_vendor.my_instrument import MyInstrument

DRIVER_REGISTRY = {
    # ... 现有驱动
    "my_instrument": MyInstrument,
}
```

#### 步骤 3: 在场景中使用

```yaml
timeline:
  - time: 5
    target: "my_instrument"
    action: "my_custom_method"
    params: { param: 123.45 }
```

### 5.3 数据库查询

测试数据存储在 SQLite 数据库：`backend/test_results.db`

#### 查询示例

```bash
sqlite3 backend/test_results.db

# 查看所有测试记录
SELECT * FROM test_runs ORDER BY id DESC LIMIT 10;

# 查看特定测试的指标数据
SELECT elapsed_time, throughput_mbps, bler 
FROM metrics_samples 
WHERE test_run_id = 1;

# 统计平均吞吐量
SELECT AVG(throughput_mbps) as avg_throughput 
FROM metrics_samples 
WHERE test_run_id = 1;
```

### 5.4 导出数据

#### 导出为 CSV

```python
# backend/export_data.py
import sqlite3
import csv

conn = sqlite3.connect('test_results.db')
cursor = conn.execute('SELECT * FROM metrics_samples WHERE test_run_id=1')

with open('metrics.csv', 'w', newline='') as f:
    writer = csv.writer(f)
    writer.writerow([desc[0] for desc in cursor.description])  # 表头
    writer.writerows(cursor.fetchall())

print("✅ 数据已导出到 metrics.csv")
```

---

## 6. 常见问题 FAQ

### Q1: 启动后无法访问前端？

**A**: 检查端口是否被占用：
```bash
lsof -i:5173  # 检查前端端口
lsof -i:8000  # 检查后端端口
```

如果被占用，终止进程：
```bash
kill -9 <PID>
```

---

### Q2: 测试启动后立即停止？

**A**: 查看后端日志：
```bash
tail -f backend/logs/app.log
```

常见原因：
- 场景文件格式错误
- 参数类型不匹配
- 仪表连接失败

---

### Q3: DUT 显示"未连接"？

**A**: 检查 ADB 连接：
```bash
adb devices
```

如果看到 `List of devices attached` 但没有设备，则：
1. 确认 USB 已连接
2. 在手机上允许 USB 调试
3. 重启 ADB: `adb kill-server && adb start-server`

---

### Q4: 实时数据不更新？

**A**: 检查 WebSocket 连接：
1. 打开浏览器开发者工具 (F12)
2. 查看 Console 标签
3. 确认无 WebSocket 错误

重启后端服务通常可以解决。

---

### Q5: 如何查看生成的测试报告？

**A**: 报告保存在：
```
backend/reports/test_report_<run_id>.html
```

直接在浏览器中打开即可。

---

### Q6: 场景执行时间不准确？

**A**: 模拟模式下可能略有偏差，这是正常的。真实硬件模式下时间更精确。

---

## 7. 故障排除

### 7.1 后端启动失败

#### 症状
```
ModuleNotFoundError: No module named 'pyvisa'
```

#### 解决方案
```bash
source venv/bin/activate  # 确保虚拟环境激活
pip install -r backend/requirements.txt
```

---

### 7.2 前端构建错误

#### 症状
```
Error: Cannot find module '@mui/material'
```

#### 解决方案
```bash
cd frontend
rm -rf node_modules package-lock.json
npm install
```

---

### 7.3 数据库锁定

#### 症状
```
sqlite3.OperationalError: database is locked
```

#### 解决方案
```bash
# 关闭所有正在访问数据库的进程
pkill -f uvicorn

# 重启后端
./start_system.sh
```

---

### 7.4 网络连接超时

#### 症状
```
pyvisa.errors.VisaIOError: VI_ERROR_TMO
```

#### 解决方案 (真实硬件模式)
1. 检查仪表 IP 地址配置
2. Ping 仪表确认网络可达：
   ```bash
   ping 192.168.1.100
   ```
3. 检查防火墙设置
4. 确认 VISA 超时时间：
   ```python
   inst.timeout = 10000  # 10秒
   ```

---

### 7.5 性能问题

#### 症状
- 前端界面卡顿
- 日志更新延迟

#### 解决方案
1. 减少日志输出频率
2. 降低采样频率：
   ```yaml
   metrics:
     interval: 1.0  # 从 0.1s 改为 1s
   ```
3. 清理历史数据：
   ```bash
   sqlite3 backend/test_results.db "DELETE FROM test_runs WHERE id < 100"
   ```

---

### 7.6 获取帮助

如果以上方法无法解决问题：

1. **查看日志**:
   - 后端: `backend/logs/app.log`
   - 前端: 浏览器 Console

2. **检查文档**:
   - 详细设计文档: `delivery/01_详细设计文档.md`
   - 接口测试报告: `delivery/02_接口测试报告.md`

3. **联系支持**:
   - GitHub Issues: https://github.com/swang430/wideband_performance/issues
   - 邮件: support@example.com

---

## 附录

### A. 快捷键参考

| 功能 | Windows/Linux | macOS |
|------|--------------|-------|
| 查找 | Ctrl + F | Cmd + F |
| 保存 | Ctrl + S | Cmd + S |
| 全选 | Ctrl + A | Cmd + A |
| 撤销 | Ctrl + Z | Cmd + Z |
| 重做 | Ctrl + Y | Cmd + Shift + Z |

### B. 目录结构

```
wideband_performance/
├── backend/
│   ├── app/              # FastAPI 应用
│   ├── core/             # 核心模块
│   ├── drivers/          # 硬件驱动
│   ├── dut/              # DUT 控制
│   ├── scenarios/        # 场景配置
│   └── tests/            # 单元测试
├── frontend/
│   └── src/
│       ├── pages/        # 页面组件
│       ├── components/   # UI 组件
│       └── contexts/     # 上下文
├── delivery/             # 交付文档
└── docs/                 # 项目文档
```

### C. 场景模板库

常用场景模板位于: `backend/scenarios/`

- `static_awgn.yaml` - 静态基准测试
- `uma_urban_macro.yaml` - 城市宏站
- `dynamic_high_speed_train.yaml` - 高铁场景
- `3gpp_7_3_sensitivity_n78.yaml` - 灵敏度测试

**建议**: 复制模板修改，而不是从零开始编写。

---

**手册结束**

**版本**: v1.0  
**维护**: Gemini System  
**更新日期**: 2026-01-17

欢迎使用终端宽带标准信道验证系统！
